# Backend API base URL.
# Local development can leave this empty when using the Vite /api proxy.
# Production on Vercel should use your Render backend URL.
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=

# Local-only Vite proxy target.
VITE_PROXY_TARGET=http://localhost:5000
